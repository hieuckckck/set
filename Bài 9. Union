#include <cmath>
#include <cstdio>
#include <vector>
#include <bits/stdc++.h>
#include <algorithm>
using namespace std;


int main() {
    int n;
    int m;
    cin>>n>>m;
    set<int>se;
    int a[n],b[m];
    for(int i=0;i<n;i++){
        cin>>a[i];
        se.insert(a[i]);
    }
    for(int i=0;i<m;i++){
        cin>>b[i];
        se.insert(b[i]);
    }
    for(auto i=se.rbegin();i!=se.rend();i++){
        cout<<*i<<" ";
    }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    return 0;
}
